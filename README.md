# Discipline

iOS-first Flutter UI/UX implementation for **Discipline – AI Addiction Control System**.

This repo is UI-only for now. Supabase integration can be added later via repositories/services in `lib/services/`.

## Run

```bash
flutter pub get --offline
flutter run
```
